class Solution:
    def mostWordsFound(self, sentences: List[str]) -> int:
        ans=[]
        for i in sentences:
            l=len(i.split(' '))
            ans.append(l)
        return max(ans)
