class Solution:
    def addToArrayForm(self, num: List[int], k: int) -> List[int]:
        c = 0
        for i in num:
            c = c*10 + i
        c += k
        c = str(c)
        ans = [int(i) for i in c]
        return ans
