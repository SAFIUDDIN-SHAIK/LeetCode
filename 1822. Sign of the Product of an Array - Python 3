class Solution:
    def arraySign(self, nums: List[int]) -> int:
        
        ans = 1
        
        for i in nums:

            if i < 0:

                ans *= -1
            
            elif not i:
                
                return 0
        
        return ans
