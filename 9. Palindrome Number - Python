class Solution:
    def isPalindrome(self, x: int) -> bool:
        
        if x>=0:
            x=str(x)
            
            if x==x[::-1]:
                return 1
            else:
                return 0
        else:
            return 0
